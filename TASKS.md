Create a 3D portfolio that showcases my past work experiences, the projects that I've worked on and some extra functionalities that I want to add in my portfolio, such as, games, tools and links to the projects that I've worked on on top of the listed projects. The project should be using `Three.js` library to render all of the 3D objects into the web page. This allows the 3D objects to be interacted by the users that visit the sight.

### Initializing the project:

- [x] Create the project using `vite.js`
  - [x] Initialize the project with `vite`
- [x] Create the view of the project using `react.js`
  - [x] Initialize the project with `react`

### Setting up Github Actions:

- [x] Create yml file for actions
- [ ] Ensure it works and builds the right files
- [ ] Point github pages to the `dist` folder

### Setting up for `Three.js`:

- [ ] Add Threejs to the project
- [ ] Use canvas to render anything on the page
- [ ] Ensure the canvas dimensions and the page width and height are the same
  - [ ] Ensure the canvas scales with the page correctly

### Learning with `Three.js`:

- [ ] Add lighting to the entire 3D world
- [ ] Create a scene
  - [ ] Create an object to show on the viewport
    - [ ] Add material
    - [ ] Add interactions (e.g. mouse clicks, hovers etc.)
    - [ ] Add animation
    - [ ] Add animation on hover
  - [ ] Import 3D object from Blender (glb file)
    - [ ] Add material to object
    - [ ] Add interactions
    - [ ] Add animation
    - [ ] Add animation on hover
  - [ ] Add interaction to go to another scene
    - [ ] Animate the transition to the next scene
- [ ] Create a different scene
  - [ ] Create an object
    - [ ] Add material
    - [ ] Add interaction to go back to the main scene
    - [ ] Add animations

### Creating the portfolio with `Three.js`:

- [ ] Create the main scene that shows all of the websites main interactive elements (about me, work experiences, projects, tools, games)
  - [ ] Create the main scene using `Three.js`
  - [ ] Create the 3D objects to show the work experiences interaction
    - [ ] Create materials needed for this interaction
  - [ ] Create the 3D objects to show the projects interaction
    - [ ] Create materials needed for this interaction
  - [ ] Create the 3D objects to show the tools interaction
    - [ ] Create materials needed for this interaction
  - [ ] Create the 3D objects to show the games interaction
    - [ ] Create materials needed for this interaction
- [ ] Create animations for each of the interactive elements in the website
